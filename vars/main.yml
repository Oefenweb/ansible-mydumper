---
# vars file for mydumper
mydumper_dependencies:
  - libglib2.0-dev
  - zlib1g-dev
  - libpcre3-dev
    # Will install the correct version, but not when using percona server
  - libmysqlclient-dev
  - libssl-dev
  - python-sphinx
  - cmake
  - patch
